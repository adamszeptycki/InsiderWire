# InsiderWire Architecture

## System Overview

InsiderWire is an intelligent insider trading signal bot that monitors SEC Form 4 filings, calculates proprietary signal scores, and delivers alerts via Slack with a web dashboard for analysis.

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         INSIDERWIRE SYSTEM                              │
└─────────────────────────────────────────────────────────────────────────┘

External Sources                 Processing Layer              Output Channels
─────────────────                ────────────────              ───────────────

┌──────────────┐                                              ┌──────────────┐
│ SEC EDGAR    │───────┐                                      │   Slack      │
│  Form 4 API  │       │                                      │  Workspace   │
└──────────────┘       │         ┌─────────────────┐         └──────────────┘
                       ├────────>│  Form 4         │               ▲
                       │         │  Processor      │               │
                       │         │  (Lambda Cron)  │               │
                       │         └────────┬────────┘               │
                       │                  │                        │
                       │                  ▼                        │
                       │         ┌─────────────────┐               │
                       │         │  XML Parser     │               │
                       │         └────────┬────────┘               │
                       │                  │                        │
                       │                  ▼                        │
                       │         ┌─────────────────┐               │
                       │         │  Scoring        │               │
                       │         │  Engine         │               │
                       │         └────────┬────────┘               │
                       │                  │                        │
                       │                  ▼                        │
                       │         ┌─────────────────┐               │
                       │         │  PostgreSQL     │───────────────┤
                       │         │  Database       │               │
                       │         └────────┬────────┘               │
                       │                  │                        │
                       │                  ▼                        │
                       │         ┌─────────────────┐               │
                       │         │  Alert Rules    │───────────────┘
                       │         │  Engine         │
                       │         └────────┬────────┘
                       │                  │
                       │                  ▼
                       │         ┌─────────────────┐         ┌──────────────┐
                       │         │  Daily Digest   │────────>│  Web         │
                       └────────>│  Aggregator     │         │  Dashboard   │
                                 │  (Lambda Cron)  │         │  (Next.js)   │
                                 └─────────────────┘         └──────────────┘
```

## Data Flow Diagram

### Form 4 Processing Pipeline

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    FORM 4 PROCESSING PIPELINE                           │
└─────────────────────────────────────────────────────────────────────────┘

1. INGESTION
   ───────────

   AWS Lambda Cron (Every 2 hours)
         │
         ▼
   ┌────────────────────┐
   │ SEC EDGAR Client   │──────> Rate Limiter (10 req/sec)
   └────────┬───────────┘
            │
            │ Fetch recent Form 4 filings (Atom/RSS feed)
            │
            ▼
   ┌────────────────────┐
   │ Filing List        │
   │ - Accession #      │
   │ - CIK              │
   │ - Company Name     │
   │ - Filing Date      │
   └────────┬───────────┘
            │
            │ For each filing...
            │
            ▼

2. PARSING
   ────────

   ┌────────────────────┐
   │ Fetch Form 4 XML   │
   └────────┬───────────┘
            │
            ▼
   ┌────────────────────┐
   │ XML Parser         │
   │                    │
   │ Extract:           │
   │ • Issuer Info      │
   │ • Insider Info     │
   │ • Transactions     │
   │ • Footnotes        │
   └────────┬───────────┘
            │
            │ Filter: P (Buy) or S (Sell) only
            │
            ▼

3. ENRICHMENT
   ───────────

   ┌────────────────────────────────────────────┐
   │          Scoring Engine                    │
   │                                            │
   │  Base Score:                               │
   │    Buy = +1, Sell = -1                     │
   │                                            │
   │  Size Multiplier:                          │
   │    log₁₀(transaction_value / $10,000)      │
   │                                            │
   │  Role Multiplier:                          │
   │    CEO/CFO/Chair = 1.5×, Others = 1.0×     │
   │                                            │
   │  First Activity Bonus:                     │
   │    +1 if first trade in 180+ days          │
   │                                            │
   │  Cluster Bonus:                            │
   │    +1 per additional insider (7-day window)│
   │                                            │
   │  Formula:                                  │
   │  Score = (Base + FirstActivity + Cluster) │
   │           × SizeMultiplier × RoleMultiplier│
   └────────────────┬───────────────────────────┘
                    │
                    ▼

4. PERSISTENCE
   ───────────

   ┌────────────────────┐
   │ Upsert Issuer      │
   └────────┬───────────┘
            │
            ▼
   ┌────────────────────┐
   │ Upsert Insider     │
   └────────┬───────────┘
            │
            ▼
   ┌────────────────────┐
   │ Upsert Transaction │──────> Dedupe Check
   │ (with score)       │        (filing + insider + date + shares + price)
   └────────┬───────────┘
            │
            ▼

5. ALERTING
   ─────────

   ┌────────────────────────────┐
   │ Check Alert Rules          │
   │                            │
   │ Urgent Alert if:           │
   │ • |Score| ≥ 5.0            │
   │ • Value ≥ $250,000         │
   └────────┬───────────────────┘
            │
            │ If triggered...
            │
            ▼
   ┌────────────────────┐
   │ Format Slack       │
   │ Block Kit Message  │
   └────────┬───────────┘
            │
            ▼
   ┌────────────────────┐
   │ Post to Slack      │
   │ via Webhook        │
   └────────┬───────────┘
            │
            ▼
   ┌────────────────────┐
   │ Record Alert       │
   │ in slack_alerts    │
   └────────────────────┘
```

### Daily Digest Pipeline

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      DAILY DIGEST PIPELINE                              │
└─────────────────────────────────────────────────────────────────────────┘

AWS Lambda Cron (Daily at 6 PM ET)
         │
         ▼
┌────────────────────────┐
│ Query Transactions     │
│ from Yesterday         │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ Group by Ticker        │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│ Calculate Aggregate Stats per Ticker  │
│                                        │
│ • Total transaction count              │
│ • Buy vs Sell count                    │
│ • Total value                          │
│ • Top insiders (by score)              │
└────────┬───────────────────────────────┘
         │
         ▼
┌────────────────────────┐
│ Format Digest Message  │
│ (Slack Block Kit)      │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ Post to Slack Thread   │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ Record Digest Alerts   │
└────────────────────────┘
```

## Database Schema

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          DATABASE SCHEMA                                │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐
│       issuers            │
├──────────────────────────┤
│ id (uuid, PK)            │
│ cik (text, unique)       │◄──────────┐
│ ticker (text)            │           │
│ company_name (text)      │           │
│ created_at (timestamp)   │           │
│ updated_at (timestamp)   │           │
└────────────┬─────────────┘           │
             │                         │
             │ 1:N                     │ N:1
             │                         │
             ▼                         │
┌──────────────────────────┐           │
│       insiders           │           │
├──────────────────────────┤           │
│ id (uuid, PK)            │           │
│ issuer_id (uuid, FK)     │───────────┘
│ name (text)              │◄──────────┐
│ title (text)             │           │
│ created_at (timestamp)   │           │
│ updated_at (timestamp)   │           │
└────────────┬─────────────┘           │
             │                         │
             │ UNIQUE(name, issuer_id) │
             │                         │
             │ 1:N                     │
             │                         │
             ▼                         │
┌──────────────────────────────────────┤
│       transactions                   │
├──────────────────────────────────────┤
│ id (uuid, PK)                        │
│ filing_accession (text)              │
│ insider_id (uuid, FK)                │───────────┘
│ issuer_id (uuid, FK)                 │
│ transaction_date (date)              │
│ transaction_code (text)              │  P = Buy, S = Sell
│ shares (decimal)                     │
│ price (decimal)                      │
│ transaction_value (decimal)          │  shares × price
│ post_transaction_shares (decimal)    │
│ is_direct_ownership (boolean)        │
│ is_10b5_1 (boolean)                  │
│ signal_score (decimal)               │  ◄─── Calculated score
│ created_at (timestamp)               │
│ updated_at (timestamp)               │
└────────────┬─────────────────────────┘
             │
             │ UNIQUE(filing_accession, insider_id,
             │        transaction_date, shares, price)
             │
             │ 1:N
             │
             ▼
┌──────────────────────────┐
│     slack_alerts         │
├──────────────────────────┤
│ id (uuid, PK)            │
│ transaction_id (uuid, FK)│
│ issuer_id (uuid, FK)     │
│ alert_type (text)        │  'urgent' or 'digest'
│ slack_thread_ts (text)   │
│ slack_message_ts (text)  │
│ posted_at (timestamp)    │
│ created_at (timestamp)   │
│ updated_at (timestamp)   │
└──────────────────────────┘
```

## Component Architecture

### Core Domain Modules

```
packages/core/src/domain/
│
├── sec/                          # SEC EDGAR Integration
│   ├── client.ts                 # API client with rate limiting
│   ├── parser.ts                 # Form 4 XML parser
│   └── types.ts                  # Type definitions
│
├── scoring/                      # Signal Score Calculation
│   ├── calculator.ts             # Score calculation logic
│   └── rules.ts                  # Alert rules and thresholds
│
├── slack/                        # Slack Integration
│   ├── client.ts                 # Webhook client
│   └── formatters.ts             # Message formatting
│
└── pipeline/                     # Processing Orchestration
    ├── processor.ts              # Main Form 4 processor
    └── aggregator.ts             # Daily digest aggregator
```

### Request Flow: Web Dashboard

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     WEB DASHBOARD REQUEST FLOW                          │
└─────────────────────────────────────────────────────────────────────────┘

User Browser                  Next.js App              Database
──────────────               ─────────────            ──────────

┌──────────────┐
│ User visits  │
│ /dashboard   │
└──────┬───────┘
       │
       │ HTTP GET
       │
       ▼
┌──────────────────┐
│ Next.js SSR      │
│ Auth Check       │──────> Better Auth Session
└──────┬───────────┘
       │
       │ Authenticated?
       │
       ▼
┌──────────────────┐
│ Load Dashboard   │
│ Page Component   │
└──────┬───────────┘
       │
       │ tRPC Query:
       │ insiders.highlights()
       │
       ▼
┌──────────────────┐
│ tRPC Router      │
│ Handler          │
└──────┬───────────┘
       │
       │ SQL Query
       │
       ▼
┌──────────────────┐              ┌──────────────┐
│ Database Query   │─────────────>│ PostgreSQL   │
│ getRecentHighScore│              └──────┬───────┘
│ Transactions     │                      │
└──────┬───────────┘                      │
       │ ◄────────────────────────────────┘
       │ Results
       │
       ▼
┌──────────────────┐
│ Serialize &      │
│ Return Data      │
└──────┬───────────┘
       │
       │ JSON Response
       │
       ▼
┌──────────────────┐
│ React Component  │
│ Renders UI       │
└──────────────────┘
       │
       │ HTML
       │
       ▼
┌──────────────────┐
│ User sees        │
│ Dashboard        │
└──────────────────┘
```

## Scoring Algorithm Explained

### Formula

```
Signal Score = (BaseScore + FirstActivityBonus + ClusterBonus)
               × SizeMultiplier
               × RoleMultiplier

Where:
  BaseScore           = +1 (buy) or -1 (sell)
  SizeMultiplier      = log₁₀(transaction_value / $10,000)
  RoleMultiplier      = 1.5 if CEO/CFO/Chair, else 1.0
  FirstActivityBonus  = +1 if first trade in ≥180 days, else 0
  ClusterBonus        = count of other insiders trading same ticker within ±7 days
```

### Example Calculations

#### Example 1: Strong Buy Signal

```
Transaction Details:
─────────────────────
• Insider: CEO of ACME Corp
• Action: Buy
• Value: $500,000
• First trade in 6+ months: Yes
• Other insiders also buying: 2 others within 7 days

Calculation:
────────────
BaseScore          = +1  (buy)
FirstActivityBonus = +1  (first trade in 180+ days)
ClusterBonus       = +2  (2 other insiders)
SizeMultiplier     = log₁₀(500000 / 10000) = log₁₀(50) = 1.70
RoleMultiplier     = 1.5 (CEO)

Score = (1 + 1 + 2) × 1.70 × 1.5
      = 4 × 1.70 × 1.5
      = 10.20

Result: 🚀 Strong Buy Signal (+10.20)
```

#### Example 2: Moderate Sell Signal

```
Transaction Details:
─────────────────────
• Insider: Director
• Action: Sell
• Value: $150,000
• Regular trading pattern
• No cluster activity

Calculation:
────────────
BaseScore          = -1  (sell)
FirstActivityBonus = 0   (regular activity)
ClusterBonus       = 0   (no other insiders)
SizeMultiplier     = log₁₀(150000 / 10000) = log₁₀(15) = 1.18
RoleMultiplier     = 1.0 (director, not C-suite)

Score = (-1 + 0 + 0) × 1.18 × 1.0
      = -1.18

Result: ⚠️ Moderate Sell Signal (-1.18)
```

## Deployment Architecture (AWS)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        AWS INFRASTRUCTURE (SST v3)                      │
└─────────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────────┐
│                          Route 53 (DNS)                                │
└────────────────┬───────────────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────────────┐
│                    CloudFront (CDN)                                    │
└────────────────┬───────────────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────────────┐
│                    Next.js App (Lambda@Edge)                           │
│                                                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐               │
│  │  Dashboard   │  │    API       │  │    Auth      │               │
│  │    Pages     │  │   Routes     │  │   Routes     │               │
│  └──────────────┘  └──────────────┘  └──────────────┘               │
└────────────────────────────────────────────────────────────────────────┘
                 │
                 │ Access secrets & DB
                 │
                 ▼
┌────────────────────────────────────────────────────────────────────────┐
│                    AWS Secrets Manager                                 │
│  • DB_URL                                                              │
│  • SLACK_WEBHOOK_URL                                                   │
│  • SEC_EDGAR_USER_AGENT                                                │
│  • BETTER_AUTH_SECRET                                                  │
└────────────────────────────────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────────────┐
│                         EventBridge Rules                              │
│                                                                        │
│  ┌─────────────────────┐         ┌─────────────────────┐             │
│  │ Form4Processor Cron │         │ DailyDigest Cron    │             │
│  │ (Every 2 hours)     │         │ (Daily 6 PM ET)     │             │
│  └──────────┬──────────┘         └──────────┬──────────┘             │
└─────────────┼────────────────────────────────┼────────────────────────┘
              │                                │
              ▼                                ▼
┌─────────────────────────┐      ┌─────────────────────────┐
│  Lambda Function        │      │  Lambda Function        │
│  form4-processor        │      │  daily-digest           │
│                         │      │                         │
│  • Poll SEC EDGAR       │      │  • Query yesterday's    │
│  • Parse Form 4         │      │    transactions         │
│  • Calculate scores     │      │  • Group by ticker      │
│  • Save to DB           │      │  • Post digest to Slack │
│  • Post urgent alerts   │      │                         │
└─────────────┬───────────┘      └─────────────┬───────────┘
              │                                │
              └────────────┬───────────────────┘
                           │
                           ▼
┌────────────────────────────────────────────────────────────────────────┐
│                      PostgreSQL Database (RDS)                         │
│                                                                        │
│  Tables:                                                               │
│  • issuers                                                             │
│  • insiders                                                            │
│  • transactions                                                        │
│  • slack_alerts                                                        │
│  • user (Better Auth)                                                  │
│  • session (Better Auth)                                               │
│  • account (Better Auth)                                               │
│  • organizations (Better Auth)                                         │
└────────────────────────────────────────────────────────────────────────┘
```

## Security & Authentication Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       AUTHENTICATION FLOW                               │
└─────────────────────────────────────────────────────────────────────────┘

1. User Registration/Login
   ─────────────────────────

   User Browser              Better Auth              Database
   ────────────              ───────────             ─────────

   ┌──────────┐
   │ Sign up  │
   └────┬─────┘
        │ POST /api/auth/sign-up
        │ {email, password}
        │
        ▼
   ┌──────────────────┐
   │ Better Auth      │
   │ Handler          │
   └────┬─────────────┘
        │
        │ Hash password
        │ Create user record
        │
        ▼
   ┌──────────────────┐                ┌──────────┐
   │ Save User        │───────────────>│ Database │
   └────┬─────────────┘                └──────────┘
        │
        │ Create session
        │
        ▼
   ┌──────────────────┐
   │ Set session      │
   │ cookie           │
   └────┬─────────────┘
        │
        │ 302 Redirect
        │ /dashboard
        ▼
   ┌──────────────────┐
   │ Dashboard        │
   └──────────────────┘


2. Protected Route Access
   ───────────────────────

   ┌──────────────┐
   │ Request      │
   │ /dashboard   │
   └──────┬───────┘
          │
          ▼
   ┌──────────────────┐
   │ Next.js          │
   │ Middleware       │
   └──────┬───────────┘
          │
          │ Check session cookie
          │
          ▼
   ┌──────────────────┐
   │ Better Auth      │
   │ Verify Session   │
   └──────┬───────────┘
          │
          ├─────> Valid?
          │        │
          │        ▼
          │     ┌──────────────┐
          │     │ Load Page    │
          │     └──────────────┘
          │
          └─────> Invalid?
                   │
                   ▼
                ┌──────────────┐
                │ Redirect to  │
                │ /auth/sign-in│
                └──────────────┘


3. tRPC API Protection
   ────────────────────

   ┌──────────────┐
   │ tRPC Query   │
   │ insiders.list│
   └──────┬───────┘
          │
          ▼
   ┌──────────────────┐
   │ tRPC Context     │
   │ Builder          │
   └──────┬───────────┘
          │
          │ Extract session from cookie
          │
          ▼
   ┌──────────────────┐
   │ Check if public  │
   │ or protected     │
   │ procedure        │
   └──────┬───────────┘
          │
          ├─────> Public?
          │        │
          │        ▼
          │     ┌──────────────┐
          │     │ Execute      │
          │     │ Handler      │
          │     └──────────────┘
          │
          └─────> Protected?
                   │
                   ▼
                ┌──────────────┐
                │ Verify user  │
                │ authenticated│
                └──────┬───────┘
                       │
                       ├─────> Yes
                       │        │
                       │        ▼
                       │     ┌──────────────┐
                       │     │ Execute      │
                       │     │ Handler      │
                       │     └──────────────┘
                       │
                       └─────> No
                                │
                                ▼
                             ┌──────────────┐
                             │ Return       │
                             │ UNAUTHORIZED │
                             └──────────────┘
```

## Monitoring & Observability

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     MONITORING & LOGGING                                │
└─────────────────────────────────────────────────────────────────────────┘

Lambda Functions              CloudWatch                 Alerts
────────────────             ──────────                ───────

┌──────────────────┐
│ Form4Processor   │
│                  │
│ console.log()    │────────> CloudWatch Logs
│ console.error()  │          • /aws/lambda/Form4Processor
└──────────────────┘          • Log retention: 30 days
                              └──────┬─────────────────────┐
                                     │                     │
                                     ▼                     │
                              CloudWatch Metrics           │
                              • Invocation count           │
                              • Duration                   │
                              • Errors                     │
                              • Throttles                  │
                                     │                     │
                                     │                     │
                                     ▼                     │
                              CloudWatch Alarms            │
                              • Error rate > 10%           │
                              • Duration > 2 min           │
                                     │                     │
                                     ▼                     │
                              SNS Topic                    │
                              • Email/SMS alerts           │
                                                           │
┌──────────────────┐                                      │
│ DailyDigest      │                                      │
│                  │                                      │
│ console.log()    │──────────────────────────────────────┘
│ console.error()  │
└──────────────────┘


Database Monitoring          Application Monitoring
───────────────────         ──────────────────────

RDS Metrics                  Custom Metrics
• CPU utilization            • Transactions processed
• Connections                • Alerts posted
• Storage used               • API response times
• IOPS                       • Error rates by type
                             • Form 4 parse failures
```

## Error Handling Strategy

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         ERROR HANDLING                                  │
└─────────────────────────────────────────────────────────────────────────┘

Level 1: SEC API Errors
────────────────────────
• 429 Too Many Requests → Wait and retry with exponential backoff
• 500 Server Error      → Skip filing, log error, continue with next
• Invalid XML           → Log parsing error, continue with next filing
• Network timeout       → Retry up to 3 times, then skip

Level 2: Database Errors
────────────────────────
• Connection failed     → Retry with exponential backoff, alert if persistent
• Constraint violation  → Log and skip (likely duplicate)
• Query timeout         → Retry once, alert if fails

Level 3: Slack API Errors
─────────────────────────
• Webhook 404           → Alert admin via logs (misconfigured webhook)
• Rate limited          → Queue message for retry
• Message too large     → Truncate and retry

Level 4: Lambda Function Errors
────────────────────────────────
• Timeout               → Log partial progress, alert admin
• Out of memory         → Reduce batch size, alert admin
• Permission denied     → Alert admin (IAM issue)

Graceful Degradation:
─────────────────────
If Slack is down → Continue processing, save alerts to DB for manual review
If DB is down    → Lambda fails, EventBridge will retry automatically
If SEC is down   → Skip execution, will catch up on next scheduled run
```

## Performance Considerations

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    PERFORMANCE OPTIMIZATIONS                            │
└─────────────────────────────────────────────────────────────────────────┘

1. Database
   ────────
   ✓ Indexes on frequently queried columns
     • transactions(transaction_date)
     • transactions(issuer_id, transaction_date)
     • transactions(insider_id)
   ✓ Composite unique constraints for fast dedupe checks
   ✓ Connection pooling (via Drizzle)

2. SEC API
   ────────
   ✓ Rate limiting (10 req/sec) to avoid 429 errors
   ✓ Process filings in batches
   ✓ Concurrent fetching with rate limiter

3. Lambda Functions
   ──────────────────
   ✓ Memory: 1024 MB (Form4Processor), 512 MB (DailyDigest)
   ✓ Timeout: 5 minutes (Form4Processor), 2 minutes (DailyDigest)
   ✓ Cold start optimization: Minimal dependencies

4. Web Dashboard
   ──────────────
   ✓ Server-side rendering (SSR) for initial page load
   ✓ React Query caching via tRPC
   ✓ Pagination for large result sets
   ✓ Debounced filter inputs
```

---

*Last updated: 2026-01-11*
